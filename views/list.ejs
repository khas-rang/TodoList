<%- include("header") -%>

  <div class="box" id="heading">
    <h1 name="thetitle"> <%= listTitle %> </h1>
  </div>

  <div class="box">
    <% for (let i=0; i<newListItems.length; i++) { %>
      <form action="/delete" method="post" >
      <div class="item">
        <input name="checkbox" type="checkbox" value="<%= newListItems[i]._id %>" onChange="this.form.submit()">
        <p><%=  newListItems[i].name %></p>
        <input type="hidden" name="listname" value="<%= thelistname %>">
      </div>
    </form>
      <% } %>

      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
        <button type="submit" name="list" value="<%= newList_title %>">+</button>
      </form>
  </div>

<%- include("footer") -%>
